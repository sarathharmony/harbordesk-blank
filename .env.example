# Copy to .env and adjust as needed. Never commit a real .env.

# Backend (packages/backend) — port the NestJS API listens on.
PORT=4000

# Frontend (packages/frontend) — override the API proxy target if the
# backend is not on localhost:4000 (e.g. a remote dev VM).
VITE_API_TARGET=http://localhost:4000
